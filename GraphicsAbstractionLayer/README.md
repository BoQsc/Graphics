A unified abstraction layer that hides the platform-specific details from the user. The key is to create an API that exposes common graphics operations such as:

Drawing shapes (e.g., rectangles, circles, lines)

Rendering text

Handling images

Creating windows (if necessary)

Managing colors, clipping, and transformation matrices
